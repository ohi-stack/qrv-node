{
  "name": "qrv-node",
  "version": "1.3.0-rc.1",
  "description": "Canonical QR-V public production platform for qrv.network with verification, issuer lifecycle, registry UI, docs, status, compiled Sites frontend runtime, authoritative production sitemap, and isolated multi-builder development lanes.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "npm run build:web",
    "start": "node server.js",
    "dev": "npm run build && node --watch server.js",
    "dev:web": "vite --host 0.0.0.0",
    "dev:chatgpt": "node scripts/dev-lane.mjs chatgpt-sites",
    "dev:aistudio": "node scripts/dev-lane.mjs google-ai-studio",
    "dev:integration": "node scripts/dev-lane.mjs integration",
    "build:web": "vite build",
    "check:dist": "node scripts/check-dist.mjs",
    "check:lanes": "node scripts/check-development-lanes.mjs",
    "check:sitemap": "node scripts/check-production-sitemap.mjs",
    "check:tier1": "node scripts/check-tier1-runtime.mjs",
    "test:routing": "node scripts/test-spa-routing.mjs",
    "lint": "node --check server.js && node --check scripts/live-acceptance.mjs && node --check scripts/test-spa-routing.mjs && node --check scripts/dev-lane.mjs && node --check scripts/check-development-lanes.mjs && node --check scripts/check-production-sitemap.mjs && node --check scripts/check-tier1-runtime.mjs && node --check src/verification.js && node scripts/check-web.mjs",
    "check": "npm run lint && npm run check:lanes && npm run check:sitemap && npm run check:tier1",
    "smoke": "node -e \"const base=process.env.QRV_NODE_URL||'http://127.0.0.1:3000'; Promise.all(['/healthz','/readyz','/version'].map(p=>fetch(base+p).then(async r=>({status:r.status,body:await r.json()})))).then(console.log)\"",
    "acceptance:live": "node scripts/live-acceptance.mjs",
    "validate:prod": "npm run check && npm run build && npm run check:dist && npm run test:routing"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "firebase": "^12.19.0",
    "helmet": "^8.1.0",
    "lucide-react": "^0.468.0",
    "qrcode": "^1.5.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
