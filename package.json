{
  "name": "qrv-node",
  "version": "1.3.0-rc.1",
  "description": "Canonical QR-V public production platform for qrv.network with verification, issuer lifecycle, registry UI, docs, status, compiled Sites frontend runtime, and isolated multi-builder development lanes.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "npm run build:web",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:web": "vite --host 0.0.0.0",
    "dev:chatgpt": "node scripts/dev-lane.mjs chatgpt-sites",
    "dev:aistudio": "node scripts/dev-lane.mjs google-ai-studio",
    "dev:integration": "node scripts/dev-lane.mjs integration",
    "build:web": "vite build",
    "check:dist": "node scripts/check-dist.mjs",
    "check:lanes": "node scripts/check-development-lanes.mjs",
    "test:routing": "node scripts/test-spa-routing.mjs",
    "lint": "node --check server.js && node --check scripts/live-acceptance.mjs && node --check scripts/test-spa-routing.mjs && node --check scripts/dev-lane.mjs && node --check scripts/check-development-lanes.mjs && node --check src/verification.js && node scripts/check-web.mjs",
    "check": "npm run lint && npm run check:lanes",
    "smoke": "node -e \"const base=process.env.QRV_NODE_URL||'http://127.0.0.1:3000'; Promise.all(['/healthz','/readyz','/version'].map(p=>fetch(base+p).then(async r=>({status:r.status,body:await r.json()})))).then(console.log)\"",
    "acceptance:live": "node scripts/live-acceptance.mjs",
    "validate:prod": "npm run check && npm run check:routes && npm run build && npm run check:dist && npm run test:routing",
    "check:routes": "node scripts/validate-route-manifest.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.1.0",
    "lucide-react": "^0.468.0",
    "qrcode": "^1.5.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
